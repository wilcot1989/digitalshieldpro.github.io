{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  {{- range .Data.Pages }}
  {{- $skip := false }}
  {{/* Skip taxonomy lists entirely (the /categories/ /tags/ index pages) */}}
  {{- if eq .Kind "taxonomy" }}{{ $skip = true }}{{ end }}
  {{/* Skip ALL tag-term pages (thin, near-duplicate) */}}
  {{- if and (eq .Kind "term") (eq .Type "tags") }}{{ $skip = true }}{{ end }}
  {{/* Skip category-term pages with fewer than 3 articles */}}
  {{- if and (eq .Kind "term") (eq .Type "categories") (lt (len .Pages) 3) }}{{ $skip = true }}{{ end }}
  {{/* Skip blog/posts index list pages */}}
  {{- if and (eq .Kind "section") (in (slice "posts" "blog") .Section) }}{{ $skip = true }}{{ end }}
  {{/* Skip pages flagged noindex/draft/disabled */}}
  {{- if .Params.noindex }}{{ $skip = true }}{{ end }}
  {{- if .Draft }}{{ $skip = true }}{{ end }}
  {{- if and .Sitemap (eq .Sitemap.Disable true) }}{{ $skip = true }}{{ end }}
  {{- if not $skip }}
  <url>
    <loc>{{ .Permalink }}</loc>
    {{- if not .Lastmod.IsZero }}
    <lastmod>{{ safeHTML ( .Lastmod.Format "2006-01-02T15:04:05-07:00" ) }}</lastmod>
    {{- end }}
    {{- $priority := "0.6" }}
    {{- if eq .Kind "home" }}{{ $priority = "1.0" }}
    {{- else if and (eq .Kind "term") (eq .Type "categories") }}{{ $priority = "0.8" }}
    {{- else if eq .Kind "page" }}{{ $priority = "0.7" }}
    {{- end }}
    <priority>{{ $priority }}</priority>
    <changefreq>weekly</changefreq>
  </url>
  {{- end }}
  {{- end }}
</urlset>
